import { CaptionKeyDirective } from './caption-key.directive';

describe('CaptionKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptionKeyDirective();
    expect(directive).toBeTruthy();
  });
});
